# Ignore Python virtual environment
/venv/

# Ignore node modules
node_modules/
package-lock.json

# Ignore Python cache files
__pycache__/
*.pyc
*.pyo
*.pyd

# Ignore user-uploaded models
models/coqui/*
models/piper/*
models/kokoro/*

# Ignore audio files
static/audio_cache/*

# Ignore build dirs
.flatpak-builder
build-dir
build
dist

# Saved users
users/

# 前端构建产物已提交到仓库（供 Docker 镜像直接使用，无需在容器内 npm build）
# 如需重新构建：本地执行 `npm install && npm run build` 后提交变更
# 注意：static/icons 仅保留 css/ 与 webfonts/，其余 svg/scss/sprites 运行时不需要